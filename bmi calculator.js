// bmi health ko measure krta h uskka simple sa formula h weight/ (hight*hight)
// function banaou that calculate bmi
// note weight kg mn hona chaiye and hight meters mn

function bmi(w, h) {
    return w /(h*h);
}
console.log(bmi(45,2.4).toFixed(2));
